/*
 * main.c
 *
 *  Created on: Mar 13, 2020
 *      Author: marcl
 */
#include<stdio.h>
#include<string.h>
#include<unistd.h> // sleep
#include<sys/socket.h>
#include<arpa/inet.h>
#include"debug.h"
#include<errno.h> // strerror
#include"server_addr.h"

int connect_to_remote_server(){
	int socket_desc;
	int myerr;
	struct sockaddr_in server;
	server.sin_addr.s_addr = inet_addr(SRV_IP);
	server.sin_family = AF_INET;
	server.sin_port = htons(SRV_PORT);

	//Creating socket
	socket_desc = socket(AF_INET , SOCK_STREAM , 0);
	//If socket creation failed
	while (socket_desc == -1){
		myerr = errno;
		error_printf("Could not create socket, error num : %d (%s)\n", myerr, strerror(myerr));
		socket_desc = socket(AF_INET , SOCK_STREAM , 0);
	}

	//Connect to remote server
	while (connect(socket_desc , (struct sockaddr *)&server , sizeof(server)) == -1){  //If connecting to server failed
		myerr = errno;
		error_printf("connect error, error num : %d (%s)\n", myerr, strerror(myerr));
	}

	puts("Connected\n");
	return socket_desc;
}

int main(int argc , char *argv[]){
	int socket_desc;
	socket_desc = connect_to_remote_server();
	while(1){
		//Send some data
		char *message;
		message = "mooordu !\n";

		while( send(socket_desc , message , strlen(message) , 0) == -1){
			//puts("Send failed\n");

			int myerr = errno;
			error_printf("Send failed, error num : %d (%s)\n", myerr, strerror(myerr));

		}
		puts("Data Sent\n");
		sleep(2);
	}

	return 0;
}
